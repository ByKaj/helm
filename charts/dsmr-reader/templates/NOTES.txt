{{ include "app.name" $ | title }} has been installed!
{{ if (eq .Values.ingress.kind "IngressRoute") }}
Browse to https://{{ coalesce .Values.ingress.subDomainOverride (include "app.fullname" .) }}.{{ .Values.ingress.rootDomain }} to use your application.
{{ end }}
Make sure to update your DNS to point `{{ coalesce .Values.ingress.subDomainOverride (include "app.fullname" .) }}.{{ .Values.ingress.rootDomain }}` to the IP of your Load Balancer where Traefik is listening.

For more information about:
{{- if .Chart.Home }}
- the application: {{ .Chart.Home }}
{{- end }}
- the chart: https://github.com/bykaj/helm/charts/{{ .Chart.Name }}

Thanks!
{{- range .Chart.Maintainers }}
-{{ .Name }}
{{- end }}
